import { Route } from '@angular/router/router';
import { EnvironmentComponent } from './environment.component';

export const EnvironmentRouter: Route = {
	path: 'environment',
	component: EnvironmentComponent
};
